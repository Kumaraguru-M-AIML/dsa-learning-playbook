# 🧬 GENIUS EXPERT WORKFLOW CYCLES
**Target Paradigm:** Maximize Cognition Depth, Zero-Rot Maintenance, Hyper-Utility.

---

This playbook defines exact operational workflows used to mobilize your **51+ ingested research repositories** into real-world architectural workflows for CQ Mythos.

---

## 🔱 CYCLE 1: The "Context Hardening" Protocol
**Objective:** Block AI context decay over multi-hour coding sessions.
**Stack Elements:**
1. `gsd-build-get-shit-done` (Structure Provider)
2. `code-review-graph-main` (Token Burn Minimizer)
3. `claude-mem` (Persistence Anchor)

### 👣 Step-by-Step Execution Flow:
1.  **Initialization**: Call `gsd-build` template structure to strictly isolate current session objective. Disallow unrelated tangents.
2.  **Delta Mapping**: Prior to reading the workspace, call `code-review-graph` to map the specific "blast radius" of upcoming changes.
3.  **Focus Lock**: Only ingest files explicitly labeled within the graph blast radius, saving 80%+ token window.
4.  **Anchor State**: Every 5 steps, manually commit compressed audit logs into `claude-mem` so context can be recovered instantaneously upon API reset.

---

## 🎭 CYCLE 2: The "Persona Governance" Hierarchy
**Objective:** Multi-layer review process simulating a YC startup execution team.
**Stack Elements:**
1. `garrytan-gstack` (Team Role Definition)
2. `hex-claude-council` (Consensus Arbiter)
3. `kyegomez-swarms` (Dynamic Labor Allocation)

### 👣 Step-by-Step Execution Flow:
1.  **Role Spawn**: Boot `gstack` framework assigning the agent as [CEO / LEAD-DEV / QA-TESTER] concurrent personas.
2.  **Proposition**: Lead Dev proposes specific structural edit to `cq_mythos_v2.py`.
3.  **Adjudication**: Trigger `claude-council` calling different internal logic chains (or model APIs if enabled) to debate the change.
4.  **Deployment**: If council approves with confidence > 90%, mobilize `swarms` worker agent logic to execute file edits and automated unittesting simultaneously.

---

## 🔍 CYCLE 3: The "Active Web Synthesis" Drive
**Objective:** Ingesting breaking state-of-the-art AI papers/docs without hallucination.
**Stack Elements:**
1. `firecrawl-firecrawl` (Clean Markdown Extractor)
2. `unclecode-crawl4ai` (Fast Scraping Engine)
3. `hancengiz-youtube-transcript-mcp` (Audio Context Capture)

### 👣 Step-by-Step Execution Flow:
1.  **Vector Targeting**: Identify new documentation URLs (e.g., Anthropic release logs).
2.  **Pure Rip**: Pipe URLs into `firecrawl` to strip JavaScript garbage and produce pure semantic Markdown.
3.  **Parallel Scraping**: Use `crawl4ai` for mass repository scanning to find associated GitHub code for the docs.
4.  **RAG Load**: Directly inject the resultant sanitized Markdown into CQ Mythos' context window as "Definitive Truth Source."

---

## 🛡️ CYCLE 4: The "Reverse Forensic" Validation
**Objective:** Break apart black-box code assets safely.
**Stack Elements:**
1. `NationalSecurityAgency-ghidra` (Static Analysis Engine)
2. `radareorg-radare2` (Dynamic Command-line Forensics)

### 👣 Step-by-Step Execution Flow:
1.  **Sandbox Isolation**: Copy binary artifacts into `scratch/sandbox`.
2.  **Static Mapping**: Run headless Ghidra script to decompile high-level flow.
3.  **Emulation Test**: Fire up `r2` to walk through stack pointer and memory mutation.
4.  **Behavioral Log**: Record actual output in `docs/forensics_report.md` preventing trust of unverified binaries.

---

## 🤖 CYCLE 5: The "Autonomous Loop Lockdown"
**Objective:** Execute complex multi-phase features via unbreakable sequential recursion.
**Stack Elements:**
1. `KSDaemon-ralphy` (Worktree Isolation & Skill Prompting)
2. `michaelshimeles-ralphy` (Pure CLI Goal Triggering)
3. `intelligence_wiki` (Inter-Loop State Memory)

### 👣 Step-by-Step Execution Flow:
1.  **Spawning**: Boot the `michaelshimeles` launcher targeting the highest level objective.
2.  **Constraint Mode**: Immediately engage the `KSDaemon` prompting discipline, forcing the agent to limit focus to EXACTLY one user story per recursive loop iteration.
3.  **Isolation**: Utilize Git Worktree mechanisms from the `bin/ralphy` core to perform work away from the master HEAD, preventing accidental contamination.
4.  **Institutionalized Learning**: Before terminating the iteration loop, parse findings and forcibly write "Codebase Patterns" into our local `intelligence_wiki` so future iterations never repeat mistakes.

---
*Playbook Version: 1.1 (Ralphy Hybrid Integration)*

