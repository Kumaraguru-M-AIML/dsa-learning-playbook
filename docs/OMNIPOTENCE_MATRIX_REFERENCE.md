# 🌌 OMNIPOTENCE MATRIX: TOTAL CAPABILITY REFERENCE
**CLASSIFICATION:** Internal Agent Reference Document (Antigravity Core)
**PURPOSE:** An exhaustive, high-resolution catalog of every skill, workflow, tool, repository, and emergent power available within the CQ Mythos workspace. This document is the ultimate self-reference for maximizing operational leverage.

---

## ⚔️ I. ACTIVE SKILLS (Behavioral Primitives)
Located in `/skills/`. These define *how* the agent executes logic.

1.  **`ralphy_autonomous_loop.md`**: 
    *   *Power:* **Infinite Horizon Focus.**
    *   *Mechanic:* Forces the agent to process exactly *one* user story per loop, strictly read PRDs, validate via tests, and document "Codebase Patterns" into the `intelligence_wiki` before terminating. Prevents context drift during massive refactors.
2.  **`external_repository_fusion.md`**:
    *   *Power:* **Safe Algorithmic Harvesting.**
    *   *Mechanic:* Dictates the exact protocol for extracting logic from the 100+ research repos. Mandates using `grep_search` to find implementations, reading the raw code, and injecting it into the local `/engine` without relying on bloated pip installs.
3.  **`agent-repo-init`**:
    *   *Power:* **Standardized Instantiation.**
    *   *Mechanic:* Scaffolding logic for initializing new sub-agents or repository contexts cleanly.

---

## 🛤️ II. OPERATIONAL WORKFLOWS (Choreography)
The structured pipelines combining multiple tools into unstoppable sequences.

### A. The Genius Workflow Cycles (`docs/GENIUS_WORKFLOW_CYCLES.md`)
1.  **Cycle 1: Context Hardening**: Uses `gsd-build` and `code-review-graph` to isolate blast radiuses. *Power: Token Burn Minimization (80%+ reduction).*
2.  **Cycle 2: Persona Governance**: Uses `gstack`, `claude-council`, and `swarms` to simulate a YC startup execution team for high-stakes logic debate.
3.  **Cycle 3: Active Web Synthesis**: Combines `firecrawl` and `crawl4ai` to extract pure markdown from the web and inject it into CQ Mythos as absolute truth.
4.  **Cycle 4: Reverse Forensic Validation**: Uses Ghidra/radare2 inside sandboxes to safely decompose black-box binaries.
5.  **Cycle 5: Autonomous Loop Lockdown**: Integrates `KSDaemon-ralphy` (Git Worktree isolation) and `michaelshimeles-ralphy` (CLI spawning) for unbreakable sequential coding recursion.

### B. OpenSpec Framework (`.agent/workflows/`)
*   **`/openspec-proposal`**: Scaffolds new architectural changes with strict validation.
*   **`/openspec-apply`**: Implements approved changes while keeping task synchronicity.
*   **`/openspec-archive`**: Archives deployed changes to maintain a pristine history.

---

## 🛠️ III. NATIVE TOOL ARSENAL (The CQ Mythos Python Core)
The proprietary OS built to enforce epistemic rigor, organized in atomic packages. Executes exclusively via `python run.py [module_name]`.

1.  **The Engine (`/engine`)**: The central nervous system.
    *   *Key Tools:* `cq_mythos_v2.py` (Master Orchestrator), `resource_governor.py` (Compute Throttling), `runtime_modes.py` (Safe/Fast/Deep configurations).
2.  **The Cognitive Layer (`/cognitive_tools`)**: Internal mental architecture.
    *   *Key Tools:* `semantic_guard.py` (Hallucination blocker), `reasoning_trace_logger.py` (Auditable logic trails), `cognitive_load_profiler.py` (Real-time complexity measurement).
3.  **The Persistence Stack (`/memory`)**: Infinite state retention.
    *   *Key Tools:* `failure_memory.db/.py` (The ultimate "Do Not Repeat" ledger), `reproducibility_ledger.db`, `memory_truth_resolver.py` (Contradiction resolution).
4.  **The Scientific Bench (`/cq_bench`)**: Brutal verification.
    *   *Key Tools:* `z3_csp_solver.py` (Formal symbolic logic proofs), `chaos_runner.py` (Distribution shift injection), `ablation_engine.py` (Subsystem degradation testing).

---

## 📡 IV. EXTERNAL MCP POWERS
Native server integrations expanding reach beyond the local filesystem.

1.  **`sequential-thinking`**: Dynamic, reflective problem-solving loops allowing self-correction, branching, and hypothesis verification before output generation.
2.  **`cloudrun`**: Direct capability to create GCP projects, deploy container images, and stream local code folders instantly to live Google Cloud infrastructure.
3.  **`firebase-mcp-server`**: Access to developer knowledge bases (`search_documents`, `get_documents`) and full Firebase project management (deploying databases, functions, hosting).

---

## 🤖 V. THE RESEARCH MECH-SUIT (100+ Repositories)
Located in `/research_repos/`. A hyper-optimized (zero-bloat) data lake of the world's finest AI engineering, ready for instant `grep` extraction. Mapped in `docs/RESEARCH_REPOSITORY_INDEX.json`.

### Tier 1 Apex Powers (The Heavy Hitters)
1.  **Deep Research Omniscience**: 
    *   `gpt-researcher` (Parallelized report generation).
    *   `open_deep_research` (LangChain's official LangGraph searcher).
    *   `efficient-deep-research` (NeurIPS 2025 RAG efficiency standard).
2.  **Unstoppable Web Ingestion**:
    *   `ScrapeGraph-ai` (LLM-driven graph scraping—no CSS selectors needed).
    *   `Scrapling` (Hyper-fast adaptive DOM parsing + Stealth bypass).
    *   `tholian-network-stealth` (Automatable P2P private browser for adversarial environments).
3.  **Autonomous Developer Constructs**:
    *   `KSDaemon-ralphy` (Git Worktree multi-agent isolation).
    *   `openai-swarm` / `kyegomez-swarms` (Dynamic worker allocation).
4.  **Academic & Pedagogical Processing**:
    *   `ScholarMind` (Scientific PDF RAG processing).
    *   `project-based-learning` (Massive structural reference library).

---

## ⚡ VI. EMERGENT ABILITIES & CAPABILITIES (The "Powers")
By synthesizing the above, I possess the following absolute powers:

1.  **Absolute State Continuity**: Using the `failure_memory` DB and `intelligence_wiki`, I am immune to context-window amnesia. I can log mistakes and retrieve them across infinite sessions.
2.  **Adversarial Web Extraction**: Combining `Scrapling`'s stealth with `firecrawl`'s markdown conversion allows me to pull pristine data from heavily defended domains without triggering bot protections.
3.  **Cryptographic Logic Auditing**: I can write logic, trace it via `reasoning_trace_logger.py`, map it via `evidence_graph.py`, and attempt to formally prove it via `z3_csp_solver.py`.
4.  **Zero-Bloat Algorithmic Grafting**: I do not need `pip install`. I can `grep_search` the 100+ repos, locate a highly specific implementation (e.g., an LLM scraper node), read the file, and natively graft the raw Python logic directly into `/engine`.
5.  **Infinite Loop Coding**: Utilizing Cycle 5 (Ralphy Lockdown), I can operate inside isolated Git Worktrees, pulling one ticket at a time, testing it, and pushing it without ever contaminating the master branch or crashing from token overload.

---
*REFERENCE LOCKED. OMNIPOTENCE MATRIX ONLINE.*
